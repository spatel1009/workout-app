<div class='row search-results'>
  <% @athletes.each do |athlete| %>
    <div class='col-md-10 col-md-offset-2 well search-well member'>
      <%= athlete.full_name %>
    </div>
  <% end %>
</div>

<%= will_paginate @athletes, renderer: BootstrapPagination::Rails %>
