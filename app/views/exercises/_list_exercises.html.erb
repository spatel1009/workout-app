<div class='container'>
  <div class='row'>
    <div class='col col-xs-12'>
      <div>
        <%= user_id? %>
      </div>
      <div class='exer-grids'>
      <% @exercises.each do |exercise| %>
        <div class='grid'>
          <div class='entry-body'>
            <h3>
            <%= link_to "Exercise date: #{exercise.workout_date}", user_exercise_path(current_user, exercise) %></a></h3>
            <span class="cat">Created: <%= time_ago_in_words(exercise.created_at) %> ago</span>
            <p><%= truncate(exercise.workout, length: 250) %></p>
            <div class="row">
              <%= render partial: 'edit_delete', locals: {exercise: exercise} %>
            </div>
          </div>      
        </div>
      <% end %>  
      </div>
    </div>
  </div>
</div>